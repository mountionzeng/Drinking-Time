<view class="page safe-top safe-bottom">
  <view class="mock-banner" aria-role="alert">
    {{badge}}
    <text class="mock-banner__detail">{{detail}}</text>
  </view>

  <view class="card">
    <text class="title">拾光 · 微信原生测试壳层</text>
    <text class="body">这一版只用来验证界面、状态和接口形状。它没有连接微信登录、没有绑定邮箱账号、没有服务器，也不会产生任何费用。</text>
  </view>

  <view class="card">
    <text class="field-label">隐私说明</text>
    <text class="body">{{consentLabel}}（版本 {{consentVersion}}）</text>
    <button class="action" bindtap="openPrivacy">查看隐私说明</button>
  </view>

  <view class="card">
    <text class="field-label">演示工作区</text>
    <text class="body" wx:if="{{!canEnterWorkspace}}">同意隐私说明后才能进入。进入后看到的仍然全部是本机演示数据。</text>
    <text class="body" wx:else>进入后可以选演示 Story、做演示聊天、编辑演示正文。</text>
    <button
      class="action action--primary"
      disabled="{{!canEnterWorkspace}}"
      bindtap="enterWorkspace"
    >进入演示工作区</button>
  </view>

  <view class="card">
    <text class="field-label">下一步需要什么</text>
    <text class="body">1. 你本人在微信公众平台确认测试号的 AppID（只需要告诉我 AppID 和小程序名称）。</text>
    <text class="body">2. AppSecret 由你本人写进服务端秘密配置，不要发到聊天里，也不会进入这个目录。</text>
    <text class="body">3. 服务端会话、邮箱绑定、真机联调和合法域名属于后续阶段，现在都还没有做。</text>
  </view>
</view>
