# 统一渲染入口改回 MJ · 2026-09-09

用户明确选择 MJ。上一版统一入口固定使用 GPT 单帧，每张预估¥1.49；这次纠正为 MJ 按四图任务计价。

- 输入1–4张：1个任务，通常返回4张，预估¥0.68。
- 输入5–8张：2个任务，通常返回8张，预估¥1.36。
- 页面显示MJ、四张一组、实际候选数量和总费用，所有付费候选保留；出现不足四张的返回或不确定失败即停止，不再追加付费。
- 显式参考使用MJ base64Array；最多四张均传递，空列表禁止默认继承。新路径不使用primaryReferenceLock，避免旧Kontext降级逻辑；缺MJ凭据不切换模型。旧严格资产锁、遮罩和精确编辑入口保留。

已验证共享任务报价/提交数量、部分失败、参考清空、路由指定MJ及禁止降级参数、四图base64输入与缺凭据不降级。类型检查、构建及架构门槛通过。自动测试使用模拟供应商，本轮未提交付费生成。
