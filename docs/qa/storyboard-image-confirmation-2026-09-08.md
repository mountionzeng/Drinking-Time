# 渲染四张：点击反馈修复（2026-09-08）

- 环境：主仓 main，唯一服务 localhost:3000；Story 1196，镜头 02。
- 修复前：两次点击“按图片要求渲染 02 的四张候选图”均触发内置浏览器 Input.dispatchMouseEvent 超时。代码使用同步 window.confirm；浏览器 dialog API 未返回可见原生弹窗，因此不能断言已捕获隐藏弹窗本体。
- 修复：图片费用确认使用页面内 Radix Dialog，继续沿用原报价与提交逻辑。取消、关闭、组件卸载或镜头集合改变将等待解析为 false；确认后重新检查当前渲染线。提交预检和生成异常另有持续可见的错误弹窗。
- 不改变默认宠物参考、原视图判定、候选采用语义或收费授权。
- 实测：重载后点击同一镜头按钮在约 0.5 秒的工具调用内显示“确认图片渲染费用”；DOM 包含镜头 02、4 张、预计 ¥0.68、取消、确认费用并渲染。点击取消后弹窗关闭，五个“渲染 4 张”按钮可见。
- 未点击费用确认，未提交新的付费生成；供应商实际出图和余额校验未验证。
- 自动检查：storyboardImageRenderPlan、StoryCardsBoard.intent、rerender 共 63 项通过，pnpm check 通过。
- 最终检查：pnpm build、pnpm feature:validate、git diff --check 均通过。

## 20:00 后实际提交排查

- 旧四张路径在服务端到达默认资产解析时出现 `[toPublicImageUrl] 主角图上传失败，cref 将跳过: fetch failed`。账本已有缺口说明：MJ 公网参考依赖 OSS 或旧 storagePut。
- 补充显式“参考素材出 1 张”入口，直接使用 GPT-image 本地参考，独立报价约 ¥1.49；不替换四张、不静默改价。成功提示使用实际张数。
- 在页面打开镜头02单张确认后，观察到弹窗关闭、渲染线1/2、随后错误弹窗；本会话工具未点击费用确认。服务端只读 `creationAgent.imageProviderStatus` 确认最后失败时间 2026-09-08T12:09:13.775Z，provider=gpt-image，message=302 图生图失败：fetch failed。当前故事最后图片仍为1792，没有新图，未重复提交。
- 另修正客户端 readableRerenderError：仅裸 fetch failed 才按连接中断处理，包含该短语的供应商完整错误和受理任务号必须原样保留；不再建议“没有候选就重试”。
- 无凭据 HEAD 检查 curl 与非沙箱 Node 均可到达 api.302.ai（200），只能证明基础连通，不能证明付费编辑请求稳定。没有再次提交生成来验证。
- 单张入口报价与默认宠物解析等77项通过；新增供应商错误保真测试后定向22项通过；类型检查、构建、功能账本验证通过。真正出图仍未完成，外部请求失败需后续排查。

## 2026-09-09 参考编辑同步分支修复

代码确认generate302GptImageEdit写死async=false，而普通文本出图早已使用async=true。新增测试先复现“收到task_id但当成空图片”和“查询失败丢任务号”，再修普通无蒙版编辑为异步提交及查询。兼容直接出图、保持蒙版像素合成；提交断线标记submissionUncertain。共75项imageGen测试通过，类型检查通过。

这只能修复明确的协议处理缺口，无法证明昨天fetch failed就是同步网关断线，也没有创建新付费任务验证。generateForMobile还未挂持久化回执回调，因此刷新恢复仍是缺口，不能声称旧请求可恢复。

## 2026-09-09 10:51 单张真实验证成功

用户在明确“镜头02，1张，预计¥1.49”后回复继续。本会话通过浏览器点击参考素材渲染02的一张图片，核对报价，再点击确认费用并渲染，仅提交一次。生成Story1196 / SH02 / publishing-v1-872f0affe88bee694dfe的新图片1793，2026-09-09T02:51:40.820Z保存，路径/api/images/1788922300804-ous8h8.png。真实像素为小猫抓咬衣袖、1024×1024完整单帧。刷新页面后仍有查看02首帧及删除02图片#1793，已打开查看。

远程备份timeout不影响已落盘图片。实际账单未读取，不把报价称作实际扣费。此次只证明单张参考通道及结果持久保存，四张MJ与提交中断回执恢复仍未解决。
