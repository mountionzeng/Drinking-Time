# 聊天照片提取追问

日期：2026-09-08。基线：`9d25882`；分支：`codex/chat-photo-extraction-choice`。

## 交互

1. 沿用聊天框回形针，选图片后保留本地缩略图，在原聊天框显示“你想提取图片里的哪一部分？”。不立即上传或调用识图。
2. 用户可以直接回复“小猫”“左边的花瓶，不要背景”，也可以点快捷回答填入输入框后编辑；点击快捷回答不会自动发送。
3. 空回复拦截；只保存图片跳过识图和生成。明确要求单张改图继续进入原有费用确认流程。
4. 发送提取要求后，focus 经过聊天上传助手、CreationEditorContext、visualAssets 路由进入视觉模型。明确类别在服务端再次过滤；物体只写 scene.fixedProps，不伪造新的物体资产类型。
5. 任意自由描述由视觉模型理解，仅建立待检查资产，不能宣称定位与识别一定准确。不完整的人物/宠物仍拒绝保存。多图原始序号在部分导入失败后保持不变。
6. 范围进入回执哈希；重复同一次提取回放，改变提取范围不误用旧回执。无 focus 的旧调用兼容原行为。
7. 切换故事清空未发送照片，旧流程不再发起后续图片处理；已经在途的原故事请求不能撤销，结果仍属于原故事。没有新增页面，不改变锁定、四视角、费用或镜头采用合同。

## 验证

- 定向回归 6 文件、58/58 用例通过；包括分类过滤（故意让 mock 视觉模型返回全部类别）、真实临时 Story 持久化、回执重放/改范围、局部上传失败保序、空回复/只保存/改图分流和追问组件静态渲染。
- `pnpm check`、`pnpm build` 通过（保留既有大 chunk 警告）；`pnpm feature:validate` 通过，40 张卡。
- 全量测试 489 文件通过、9 跳过；4 个端口监听测试文件被沙箱 EPERM 阻断（4277 用例通过、16 失败、65 跳过）。这 4 文件允许临时测试端口后单独复测 39/39 通过；不能表述成一次全量绿跑。保序修正后另重跑上述 58 项及类型/账本检查。
- 没有启动 worktree dev/preview，没有写入用户测试数据，没有提交真实识图或付费生图。
- ce-code-review 按本项目工具映射在主会话顺序自审，不是独立多代理审查。检查正确性、测试、维护性、项目规范、Agent/API 可达性、既有经验，以及输入/接口合同、可靠性、竞态、TypeScript 和失败场景。当前无剩余代码阻断项。

## 待完成

- 未合入 main、未推送。主仓会话看板另有“小游戏账号与旧故事接入”登记修改功能账本，与本任务在文件级重叠；依项目协调规则暂停合并，等待用户裁决。代码隔离保留，不能覆盖对方登记/记录。
- 浏览器工具报告 Mac 锁屏，无法截图及实点验证。解锁并协调合并后，只在主仓 3000 验证回形针、追问、快捷回答、清空/取消及换故事行为。真实自由描述准确性与猫图成图质量仍待验收，功能保持 observing。

## Post-Deploy Monitoring & Validation

负责人：本任务；窗口：合并后首次照片流程和一次切换故事检查。
健康信号：选图/空回复期间没有 importStoryMaterial、extractPhotoFeatures 或生图请求；只保存仅导入图片；选择“只提取宠物”仅新增 pet 待检查版本；同图片/范围重试不重复建立资产；切换故事后不继续导入后续图片。
失败信号：没有回答就上传、保存图片触发识图、明确类别越界、跨故事继续提交。出现时停止真实验收，保留原图/回执并修正入口；不删除用户素材，不自动重买。


## 接手与主仓真实验收（2026-09-08）

- 看板中小游戏任务已明确不占主仓账本、不开合并窗口；原照片任务停止后，本任务接手。
- `061893f` 已 fast-forward 合入 main，对应干净 worktree 和分支已删除。
- 主仓回归 49/49、check、build、feature:validate 通过。修复列表事实确认后另外 29/29 和 check 通过。
- 在 main:3000 Story 1196 实点上传三张用户猫照片。出现提取追问，快捷“只提取宠物”只填入输入框，发送才导入。原图 imageId 1784（头脸近照）、1785（全身）、1786（顶部），全部持久化入库。
- 前两张各提取一个 pet 草案；顶部单图没有建立不完整宠物，没有额外人物/场景资产。
- 通过第二个 pet 的“建立新版本”选入三图，assetId `va_TaolTDYqMJ_d` 的 v2 联合分析完成。模型误报视角/光线差异为冲突，且冲突文字中的图片编号和视角有错；人工依据原图核对后使用已整理的事实，不采纳这些错误差异描述。
- 发现列表事实无法选择推荐值，修复前后端：允许确认当前完整列表，持久化保留条目边界，不能把分号合并字符串作为新事实追加。相关回归覆盖整表确认与原单项冲突裁决兼容。
- 已确认事实，设置彩铅＋淡水彩、保留灰黑银色虎斑/M纹/浅色围脖/浓密尾巴，选择补充顶部。实际报价五图最高 ¥7.45，浏览器停在费用确认，尚未发起真实生图。
- 多图同一主体暂不自动归并，需要已有“建立新版本”交互汇集参考。功能仍 observing；不能宣称已有优质生成资产。
