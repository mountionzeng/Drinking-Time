# 小游戏版本同步

用户于 2026-09-08 指定建立独立小程序分支，跟随手机版提交更新。

- 工作分支：`codex/wechat-mobile-parity`
- 版本来源：本地主仓 `main` 的已提交版本（不包括主仓未提交修改）。用户指定其他来源或提交号时再调整。
- 建立基线：`2e52a2d`。此版本只是 Git 基线，不代表小程序已完成还原。
- 视觉目标：https://test.drinkingtime.top/login 及登录后的手机版。
- 职责：用户已明确改为小游戏。以 minigame/ 的 Canvas 与键盘入口迁移手机版交互，共用原服务权威；miniprogram/ 的纯业务状态层可复用，WXML 页面不再作为最终交付入口。
- 每天 10:00（Asia/Shanghai）检查来源提交；来源无变化保持安静。
- 只在本 worktree 干净且无 Git 操作进行时合并来源；不得自动 stash、覆盖或清除用户修改。出现冲突停止并报告，不使用 ours/theirs 批量裁决。
- Git 同步与原生功能适配分别记录：合并 Web 源码不意味着小程序已具备对应功能。每次检查新增手机版界面、接口和行为，完成适配后运行小程序独立类型检查和测试，记录尚未通过的真机项目。
- 不在此 worktree 启动服务，不写业务数据，不自动部署或提交微信审核，不修改来源分支。
- 旧微信工作区及其未提交文件保留；不自动复制、合并或删除旧分支。涉及旧会话占用的共享后端文件时先协调。
- 这是用户明确要求持续保留的同步分支；完成阶段性适配不删除它，直到用户结束此流程。

## 进度

- 2026-09-09：专属分支已实现独立 live 入口：邮箱密码/验证码登录、微信登录与明确确认后的关联、旧故事读写、正文 CAS、聊天同轮恢复、故事新建/切换、余额、来信和出生资料。复用原手机版保存/聊天状态层、五行图标、字体及两日纳音轮换；默认构建仍是显式 mock。
- AppID 为 `wxd6aeb0bc3a031d39`。测试站在 `33b5cc8` 上叠加 11 文件服务端增量；微信 AppSecret 已经官方接口验证并仅配置在测试站环境文件，微信登录开关已开启。凭据有效、无效 code 被拒绝不等于真实微信登录成功。
- 本次复验：认证/客户端专项 38 项、真实 Express/JWT/原路由与隔离本地持久化链 4 项、根与小游戏独立类型检查、mock 构建/冒烟及 `--live --wechat` 构建/替身冒烟通过。此前隔离 MySQL 并发/回滚 3 项通过；全量记录仍为 4302 通过、6 项既有图像测试失败、45 跳过，未宣称全量绿。
- 新预览包尚未上传：需要用户明确批准上传本次代码包到该 AppID 才能生成新体验二维码；不复用旧只读码。真实 wx.login、旧账号关联、中文键盘、安全区及跨端正文仍待真机验收。
- 已知还原缺口：来信自动迎面、完整气息/相关故事布局、生日滚轮，以及邮箱注册、微信绑定新邮箱、链接码和两个已有账号合并。状态保持 observing，不宣称 100% 还原。
- 2026-09-09：实现提交 `66e36f0` 后，将 main 的 `de228089473f54eeacdd586404e0fbe8dd8449df`（21 个来源提交）无冲突同步，合并提交 `fa5a1bb`。合并后专项 38 项、原手机版/真实小游戏工作区链 23 项、根与小游戏类型检查、live 微信构建/替身冒烟通过。仅同步已提交源码，不包含 main 未提交内容；测试站仍保持既有部署，未随 Git 合并自动部署。
- 本次来源包含新版余额展示、来信/生日表单和记忆日历等改动。Git 合并不代表 Canvas 已实现这些全部入口；除上述缺口外，记忆日历尚未迁移。仅在本分支清洁、主仓合并窗口空闲且无冲突时推进每日同步；自动同步不部署、不上传。
- 详细证据与发布边界见 `docs/qa/2026-09-08-minigame-live-auth.md`。
