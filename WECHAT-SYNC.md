# 小程序版本同步

用户于 2026-09-08 指定建立独立小程序分支，跟随手机版提交更新。

- 工作分支：`codex/wechat-mobile-parity`
- 版本来源：本地主仓 `main`（当前手机版改动已进入此分支）。用户指定其他来源或提交号时再调整。
- 建立基线：`2e52a2d`。此版本只是 Git 基线，不代表小程序已完成还原。
- 视觉目标：https://test.drinkingtime.top/login 及登录后的手机版。
- 职责：用户已明确改为小游戏。以 minigame/ 的 Canvas 与键盘入口迁移手机版交互，共用原服务权威；miniprogram/ 的纯业务状态层可复用，WXML 页面不再作为最终交付入口。
- 每天 10:00（Asia/Shanghai）检查来源提交；来源无变化保持安静。
- 只在本 worktree 干净且无 Git 操作进行时合并来源；不得自动 stash、覆盖或清除用户修改。出现冲突停止并报告，不使用 ours/theirs 批量裁决。
- Git 同步与原生功能适配分别记录：合并 Web 源码不意味着小程序已具备对应功能。每次检查新增手机版界面、接口和行为，完成适配后运行小程序独立类型检查和测试，记录尚未通过的真机项目。
- 不在此 worktree 启动服务，不写业务数据，不自动部署或提交微信审核，不修改来源分支。
- 旧微信工作区及其未提交文件保留；不自动复制、合并或删除旧分支。涉及旧会话占用的共享后端文件时先协调。
- 这是用户明确要求持续保留的同步分支；完成阶段性适配不删除它，直到用户结束此流程。

## 进度

- 当前可执行基线：`node minigame/scripts/build.mjs` 与 `node minigame/scripts/smoke.mjs` 通过。运行测试覆盖启动、键盘最终值、演示正文保存、聊天改稿及发送、视图切换与前后台处理。小游戏新增故事切换、滚动、未知结果查询和冲突文本复制入口；这些新增入口尚未全部自动验证或真机验收。AppID 已按用户提供配置；真实后端未接入。后端现有邮箱密码接口返回 Web Cookie，小游戏统一身份合同须与认证线协调，禁止假造 live 成功。开发者工具仍存在 .cli 初始化错误。

- 原生布局第一步：workspace 三个页面文件已加入正文常驻和聊天三档按钮切换；独立 tsc、144 项既有测试通过。尚未实现拖动、真实身份和后端，尚未真机验收。项目仍为 touristappid；提供真实 AppID 后方可推进对应账号预览。不要把本次修改称为完成迁移。

- 2026-09-08：建立独立分支与版本跟踪约定。原生还原尚未验收。
- 2026-09-08：用户将自动检查改为每天一次。首次界面对照发现：手机版 `MobileWorkspace.tsx` 使用正文常驻、聊天 peek/half/full 三档底部面板；小程序 `pages/workspace/index.wxml` 仍用聊聊/正文互斥条件渲染。下一项为面板结构迁移，需要同时保留正文草稿、冲突处理、同轮重试与 mock 标识；当前未实施、未真机验收。真实认证仍由独立后端线负责协调，不能以视觉改动代替。
